
    方法 1:浏览器中抓包+ Copy as cURL + Postman 导入(最稳)
    你必须:
    1.在 浏览器登录后，打开开发者工具 → Network.
    2.点击你触发这个接口的页面(比如点击按钮)一 找到该请求。
    3.右键请求→ Copy → copy as cURL(bash)
    4.打开 Postman →lmport →粘贴→ lmport.
    
    这样就生成了postman文档
    然后根据postman 里面找到 code ->选择你要的（php或者go 或者node）生成现成的代码
    即可执行  